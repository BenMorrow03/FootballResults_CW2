name: Build and deploy Node.js project to Azure Function App

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "18"

      - name: Install dependencies
        run: npm ci

      # If you have a build step later, uncomment:
      # - name: Build
      #   run: npm run build --if-present

      - name: Azure Login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_IDAZUREAPPSERVICE_CLIENTID_B05CB138BE3B4EA5845E30FC208E2020  }}
          tenant-id: ${{ secrets.AZURE_TENANT_IDAZUREAPPSERVICE_TENANTID_43BA06EE63D44538B4D66E6CABD5495F }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_FEBF8C2EC165439AABAC1DBB9A70F91C }}

      - name: Deploy to Azure Functions
        uses: Azure/functions-action@v1
        with:
          app-name: "functions-cw2"
          package: "."

